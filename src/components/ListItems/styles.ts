import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	container: {
		borderTopWidth: 0,
		borderRightWidth: 0,
		borderBottomWidth: 1,
		borderLeftWidth: 0,
	}
})

export default styles
